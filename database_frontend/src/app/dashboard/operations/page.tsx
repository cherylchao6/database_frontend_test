/// Function var to render the Operation Page

import { Metadata } from "next";

// Define the metadata
export const metadata: Metadata = {
  title: "Operation",
  description: "Operation Module",
};

const OperationPage = () => {
  return (
    <div>
      <h1>Operation Page</h1>
    </div>
  );
};

export default OperationPage;
